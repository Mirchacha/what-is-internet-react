# ⚠️ Важливо! Примітки для деплою

## 🚨 Якщо змінюєте назву репозиторію

Якщо ви використовуєте **іншу назву** репозиторію (не `what-is-internet-react`), обов'язково змініть:

### 1. vite.config.ts

```typescript
base: '/ВАША_НАЗВА/',  // Змініть ВАША_НАЗВА на вашу назву репозиторію
```

### 2. package.json

```json
{
  "homepage": "https://YOUR_USERNAME.github.io/ВАША_НАЗВА"
}
```

## 📝 Приклади

### Якщо ваш репозиторій: `my-internet-app`

**vite.config.ts:**
```typescript
base: '/my-internet-app/',
```

**package.json:**
```json
{
  "homepage": "https://username.github.io/my-internet-app"
}
```

### Якщо ваш репозиторій: `internet-demo`

**vite.config.ts:**
```typescript
base: '/internet-demo/',
```

**package.json:**
```json
{
  "homepage": "https://username.github.io/internet-demo"
}
```

## ✅ Швидка перевірка

Після змін обов'язково:

1. Збірка працює:
   ```powershell
   npm run build
   ```

2. Preview працює локально:
   ```powershell
   npm run preview
   ```
   Відкрийте `http://localhost:4173/what-is-internet-react/` 
   (або вашу назву)

3. Deploy працює:
   ```powershell
   npm run deploy
   ```

## ❌ Типові помилки

### Помилка: "Cannot GET /weather"

**Проблема:** `base` неправильно налаштовано в `vite.config.ts`

**Рішення:** Переконайтесь, що `base` має `/` на початку і в кінці:
```typescript
base: '/назва-репо/',  // ✅ Правильно
base: '/назва-репо',   // ❌ Неправильно (немає / в кінці)
base: 'назва-репо/',   // ❌ Неправильно (немає / на початку)
```

### Помилка: 404 на всіх сторінках крім головної

**Проблема:** GitHub Pages не підтримує SPA routing

**Рішення:** ✅ Уже налаштовано! `gh-pages` автоматично створює файли 404.html

---

**Завжди перевіряйте ці налаштування перед деплоєм!**

