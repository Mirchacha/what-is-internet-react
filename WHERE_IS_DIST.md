# 🔍 Де знаходиться папка dist?

## 📂 Коротка відповідь

**Папка `dist` НЕ існує зараз!** 

Вона створюється коли ви збираєте проект командою `npm run build`.

---

## ✅ Як створити папку dist

### Крок 1: Відкрийте PowerShell

Натисніть `Win + X` → Windows PowerShell

### Крок 2: Перейдіть в папку проекту

```powershell
cd C:\Users\User\what-is-internet-react
```

### Крок 3: Зібрати проект

```powershell
npm run build
```

⏱️ Зачекайте 30-60 секунд...

### Крок 4: Папка створена!

✅ Ви побачите повідомлення типу:
```
✓ built in 2.3s
```

✅ Папка `dist` тепер існує в:
```
C:\Users\User\what-is-internet-react\dist\
```

---

## 📁 Розташування папки dist

Після збірки структура буде така:

```
what-is-internet-react/
├── dist/              ← ТУТ! Це зібраний проект
│   ├── index.html
│   ├── assets/
│   │   ├── index-abc123.js
│   │   └── index-xyz789.css
│   └── vite.svg
├── src/               ← Вихідний код (для розробки)
├── public/
├── package.json
└── ...
```

---

## 🎯 Після створення dist

Тепер ви можете:

1. **Задеployити** на Netlify:
   - Перетягніть всю папку `dist` на https://app.netlify.com/drop

2. **Переглянути локально**:
   ```powershell
   npm run preview
   ```

---

## ❓ Чому немає папки dist?

Папка `dist` в `.gitignore`, тому:
- Вона не потрапляє у Git
- Вона не зберігається у репозиторії
- Вона збирається при потребі з `src/` коду

Це нормально! Просто запустіть `npm run build`.

---

## ⚠️ Якщо помилка при npm run build

### "npm не визначено"
→ Встановіть Node.js: [INSTALL_NODEJS_WINDOWS.md](INSTALL_NODEJS_WINDOWS.md)

### "npm install не виконано"
→ Спочатку виконайте:
```powershell
npm install
```

### "Щось не так з кодом"
→ Переконайтесь що всі файли на місці:
```powershell
dir src
```

---

## 📝 Підсумок

1. `dist` створюється командою `npm run build`
2. Вона містить готовий для деплою проект
3. Перетягніть всю папку `dist` на Netlify Drop
4. Готово! 🎉

---

**Запустіть `npm run build` зараз і все запрацює!** 🚀

