# 🪟 Windows 11 - Крок за кроком інструкція

## 📋 Передумови

Перед початком переконайтесь, що у вас встановлено:
1. **Node.js** (версія 18 або вище)
2. **npm** (йде разом з Node.js)

### Крок 1: Перевірка встановлення Node.js

1. Відкрийте **PowerShell** або **Command Prompt** (натисніть `Win + X` і виберіть "Windows PowerShell" або "Термінал")
2. Введіть команду:
```powershell
node --version
```
Якщо бачите версію (наприклад, v18.17.0) - все добре!

3. Введіть команду:
```powershell
npm --version
```
Якщо бачите версію (наприклад, 9.6.7) - все добре!

**Якщо Node.js НЕ встановлено:**
- Завантажте з https://nodejs.org/
- Встановіть **LTS версію** (Recommended)
- Перезапустіть PowerShell після встановлення

---

## 🚀 Крок за кроком запуск проекту

### Крок 2: Відкриття проекту в PowerShell

1. Натисніть **Win + X**
2. Виберіть **"Windows PowerShell"** або **"Термінал"**
3. Перейдіть до папки проекту:
```powershell
cd C:\Users\User\what-is-internet-react
```

### Крок 3: Встановлення залежностей (перший раз!)

Це потрібно зробити **ТІЛЬКИ один раз**, коли запускаєте проект вперше:

```powershell
npm install
```

⏱️ Це займе 2-3 хвилини. Ви побачите прогрес встановлення.

**Що відбувається?** npm завантажує всі потрібні бібліотеки (React, Vite, і т.д.)

---

### Крок 4: Запуск проекту

Після встановлення, запустіть проект:

```powershell
npm run dev
```

Ви побачите щось таке:
```
  VITE v5.0.8  ready in 1234 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### Крок 5: Відкриття в браузері

1. Скопіюйте адресу: `http://localhost:5173`
2. Відкрийте свій браузер (Chrome, Edge, Firefox)
3. Вставте адресу в адресний рядок та натисніть Enter

🎉 **Проект працює!** Ви побачите головну сторінку "Що таке Інтернет?"

---

## 🧪 Запуск тестів

### Відкрийте НОВИЙ термінал PowerShell

**Важливо:** залиште запущений `npm run dev` в одному терміналі, і відкрийте новий для тестів!

```powershell
# В новому PowerShell
cd C:\Users\User\what-is-internet-react
npm test
```

Тести автоматично запустяться і покажуть результат.

**Щоб зупинити тести:** натисніть `Ctrl + C`

---

## ⚠️ Типові проблеми та рішення

### Проблема 1: "npm не є внутрішньою або зовнішньою командою"
**Рішення:** Node.js не встановлено або не додано до PATH. Перевстановіть Node.js.

### Проблема 2: "npm ERR! code EACCES"
**Рішення:** Запустіть PowerShell **як Адміністратор**:
- Натисніть `Win + X`
- Виберіть "Windows PowerShell (Адміністратор)"

### Проблема 3: "Port 5173 is already in use"
**Рішення:** Порт зайнятий. Зупиніть інший процес або використайте інший порт:
```powershell
npm run dev -- --port 3000
```

### Проблема 4: Довге встановлення
**Рішення:** Це нормально! Перший раз завжди довго. Почекайте.

### Проблема 5: Помилки при встановленні
**Рішення:** Очистіть кеш npm:
```powershell
npm cache clean --force
npm install
```

---

## 📝 Основні команди (шпаргалка)

```powershell
# Встановити залежності (перший раз)
npm install

# Запустити проект
npm run dev

# Зупинити проект
Ctrl + C

# Запустити тести
npm test

# Запустити тести з покриттям
npm run test:coverage

# Зібрати production версію
npm run build

# Переглянути збірку
npm run preview
```

---

## 🎯 Що далі?

### Після запуску проекту ви можете:

1. **Переглянути сторінки:**
   - Головна (Home)
   - Погода (Weather) - потрібен дозвіл на геолокацію
   - Криптовалюти (Crypto)
   - Мережа (Network)

2. **Редагувати код:**
   - Відкрийте `src/` папку в VS Code або іншому редакторі
   - Змініть файли - зміни автоматично оновляться в браузері!

3. **Додати власні функції:**
   - Створіть нові сторінки в `src/pages/`
   - Додайте роути в `src/app/routes.tsx`

---

## 🛠️ Рекомендовані інструменти

Для комфортної роботи встановіть:

1. **Visual Studio Code** - редактор коду
   - https://code.visualstudio.com/
   - Рекомендовані розширення:
     - ES7+ React/Redux/React-Native snippets
     - ESLint
     - Prettier
     - Auto Rename Tag

2. **Git for Windows** (опційно)
   - https://git-scm.com/download/win
   - Для контролю версій коду

---

## 🆘 Потрібна допомога?

### Логи помилок
Якщо щось не працює, скопіюйте повний текст помилки з терміналу та зверніться за допомогою.

### Перезапуск
Іноді допомагає:
```powershell
# Зупинити все (Ctrl + C)
# Видалити node_modules
Remove-Item -Recurse -Force node_modules

# Видалити package-lock.json
Remove-Item -Force package-lock.json

# Повторне встановлення
npm install

# Запуск
npm run dev
```

---

## ✅ Чеклист першого запуску

- [ ] Node.js встановлено
- [ ] PowerShell відкрито
- [ ] cd в папку проекту виконано
- [ ] npm install виконано
- [ ] npm run dev запущено
- [ ] Браузер відкрито на http://localhost:5173
- [ ] Головна сторінка відображається

**Успіхів! 🚀**

