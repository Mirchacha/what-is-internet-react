# ⚡ Швидкий старт

## 1. Встановіть залежності

```bash
npm install
```

## 2. Запустіть dev сервер

```bash
npm run dev
```

Відкрийте http://localhost:5173 у браузері.

## 3. Структура проекту

```
what-is-internet-react/
├── src/
│   ├── app/              # App.tsx + routes
│   ├── pages/            # Home, Weather, Crypto, Network
│   ├── components/       # Card, Loader, ErrorMessage
│   ├── api/              # HTTP wrapper + API modules
│   ├── store/            # Zustand store
│   ├── utils/            # Format + geo
│   ├── styles/           # Globals + modules
│   └── types/            # TypeScript types
├── public/               # Favicon
├── package.json
├── vite.config.ts
└── README.md
```

## 4. API Endpoints

- **GET** `/` — Головна сторінка з описом
- **GET** `/weather` — Погода за геолокацією
- **GET** `/crypto` — Топ-10 криптовалют
- **GET** `/network` — IP + Cat fact

## 5. Технічні деталі

### HTTP Wrapper (`src/api/http.ts`)

- ✅ AbortController для скасування запитів
- ✅ Retry (1 спроба за замовчуванням)
- ✅ Timeout (10 сек)
- ✅ Обробка помилок HTTP

### Кешування

- CoinGecko кешується на 60 секунд
- Zustand store з глобальним кешем

### TypeScript

- Повна типізація всіх API відповідей
- Без `any` типу
- Strict mode увімкнено

## 6. Збірка для продакшн

```bash
npm run build
npm run preview
```

## 7. Можливі проблеми

### Геолокація не працює
- Перевірте дозволи браузера
- Використовуйте HTTPS (або localhost)

### Rate limit на CoinGecko
- Кеш на 60 секунд вже налаштовано
- Можна збільшити TTL у `src/pages/Crypto.tsx`

### Помилки типів
```bash
npm run build
```
Перевірте вивід tsc

## 8. Додаткові можливості

Додайте свої публічні API:
1. Створіть модуль у `src/api/`
2. Додайте тип у `src/types/api.d.ts`
3. Створіть сторінку у `src/pages/`
4. Додайте роут у `src/app/routes.tsx`

---

**Готово!** 🎉 Ваш проект готовий до роботи.

