# 📥 Встановлення Node.js на Windows 11

## ⚠️ Важливо!

Якщо ви бачите помилку "node is not recognized", потрібно спочатку встановити Node.js.

---

## 🚀 Крок за кроком встановлення

### Крок 1: Завантаження Node.js

1. **Відкрийте браузер** і перейдіть на:
   ```
   https://nodejs.org/
   ```

2. Ви побачите дві зелені кнопки:
   - **LTS** (Long Term Support) - рекомендовано!
   - **Current** (поточна версія)

3. **Натисніть на LTS** - вона стабільніша

4. Завантажиться файл типу `node-v20.10.0-x64.msi`

### Крок 2: Встановлення Node.js

1. **Знайдіть завантажений файл** в папці "Завантаження" (Downloads)
2. **Подвійний клік** по файлу для запуску
3. Відкриється вікно установки
4. **Натисніть "Next"** кілька разів
5. **Залиште всі опції за замовчуванням**
6. Натисніть **"Install"**
7. Може знадобитися ввести пароль адміністратора
8. Почекайте, поки завершиться встановлення
9. Натисніть **"Finish"**

### Крок 3: Перезапуск PowerShell

**ВАЖЛИВО!** Після встановлення Node.js:

1. **Закрийте** всі відкриті PowerShell/термінали
2. **Відкрийте новий** PowerShell:
   - Натисніть `Win + X`
   - Виберіть "Windows PowerShell"

### Крок 4: Перевірка встановлення

У новому PowerShell введіть:

```powershell
node --version
```

Ви повинні побачити щось таке:
```
v20.10.0
```

Якщо бачите версію - **Node.js успішно встановлено!** 🎉

Також перевірте npm:
```powershell
npm --version
```

Повинно показати:
```
10.2.3
```

---

## ❌ Проблеми з установкою

### Якщо після перезапуску PowerShell все ще не працює:

1. **Перезавантажте комп'ютер** (іноді допомагає)
2. Перевірте вручну:
   - Натисніть `Win + R`
   - Введіть: `C:\Program Files\nodejs\`
   - Натисніть Enter
   - Якщо папка відкриється - Node.js встановлено

3. **Додайте вручну до PATH** (якщо не встановилося автоматично):
   - Натисніть `Win + X`, виберіть "Система" → "Додаткові параметри системи"
   - Натисніть "Змінні середовища"
   - У "Системні змінні" знайдіть "Path" → "Редагувати"
   - "Створити" → Введіть: `C:\Program Files\nodejs\`
   - Ок → Ок → Ок
   - **Перезапустіть PowerShell**

---

## ✅ Після встановлення

Коли Node.js працює, поверніться до:
```
WINDOWS_11_STEP_BY_STEP.md
```

і продовжіть з Кроку 2 (npm install).

---

## 📝 Примітки

- **Node.js включає npm** - додатково встановлювати не потрібно
- Встановлюється в `C:\Program Files\nodejs\`
- За замовчуванням додається в PATH автоматично
- Рекомендована версія: **LTS** (Long Term Support)

---

## 🆘 Ще потрібна допомога?

1. Переконайтесь, що завантажили з https://nodejs.org/ (офіційний сайт)
2. Спробуйте встановити версію "Current" замість "LTS"
3. Перевірте права адміністратора
4. Вимкніть антивірус тимчасово під час встановлення

**Коли Node.js встановлено, можна запускати проект!** 🚀

